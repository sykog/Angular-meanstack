import { LogOutDirective } from './log-out.directive';

describe('LogOutDirective', () => {
  it('should create an instance', () => {
    const directive = new LogOutDirective();
    expect(directive).toBeTruthy();
  });
});
